- name: Import and save imported configs
  hosts: localhost
  tags: [always]
  roles:
    - importer

- name: Prepare partitions
  hosts: localhost
  tags: [particionamento]
  roles:
    - particionamento

- name: Install base operating system
  hosts: localhost
  tags: [instalacao]
  roles:
    - sistema

- name: Setup config
  hosts: localhost
  tags: [users]
  roles:
    - users

- name: Setup AUR
  hosts: localhost
  tags: [aur]
  roles:
    - aur

- name: Configura os dotfiles do sistema
  tags: dotfiles
  hosts: localhost
  roles:
    - role: dotfiles

- name: Configura a região do sistema
  tags: region
  hosts: localhost
  roles:
    - role: region

- name: Configura o bootloader do sistema
  hosts: localhost
  tags: bootloader
  roles:
    - role: bootloader

- name: Gera o fstab do sistema
  tags: fstab
  hosts: localhost
  roles:
    - role: fstab

- name: Setup Repos
  hosts: localhost
  tags: [repos]
  roles:
    - repos

- name: Install plugin pkgs
  hosts: localhost
  tags: [pkgs]
  roles:
    - pkgs

- name: Configure Services
  hosts: localhost
  tags: [services]
  roles:
    - services
